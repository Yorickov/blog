extends ../layouts/app.pug

block content
  .container.mt-5
    h3.text-center Posts
    if posts.length > 0
      ul
      for post in posts
        .list-group
          a.list-item(href=url('posts#show', { id: post.id }))= post.title
    else
      p Пусто
